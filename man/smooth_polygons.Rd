% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{smooth_polygons}
\alias{smooth_polygons}
\title{Smooth polygon boundaries}
\usage{
smooth_polygons(x, method = "ksmooth", smoothness = 3)
}
\arguments{
\item{x}{An \code{sf} object containing polygon or multipolygon geometries.}

\item{method}{Smoothing method passed to \code{\link[smoothr:smooth]{smoothr::smooth()}}. Common choices
include \code{"ksmooth"}, \code{"chaikin"}, \code{"spline"}, and \code{"densify"}.}

\item{smoothness}{Smoothing parameter passed to the selected method through
\code{\link[smoothr:smooth]{smoothr::smooth()}}.}
}
\value{
An \code{sf} object with smoothed geometries and the same attribute
columns as \code{x}.
}
\description{
Smooths the geometries of an \code{sf} object using \code{\link[smoothr:smooth]{smoothr::smooth()}}. Attribute
columns are retained. The available methods and the interpretation of
\code{smoothness} are defined by \code{smoothr}.
}
\examples{
square <- sf::st_sf(
  region = "example",
  geometry = sf::st_sfc(sf::st_polygon(list(matrix(
    c(0, 0, 1, 0, 1, 1, 0, 1, 0, 0), ncol = 2, byrow = TRUE
  ))))
)
smoothed <- smooth_polygons(square, method = "ksmooth", smoothness = 3)
}
