% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{edit_silhouette}
\alias{edit_silhouette}
\title{Interactively edit silhouette lines}
\usage{
edit_silhouette(x, layer = "silhouette", click_tolerance = 0.025)
}
\arguments{
\item{x}{An \code{sf} object containing \code{LINESTRING} or \code{MULTILINESTRING}
geometries, or an atlas list containing such a layer.}

\item{layer}{Name of the silhouette layer when \code{x} is an atlas list. Defaults
to \code{"silhouette"}; \code{"cortex_silhouette"} is another common choice.}

\item{click_tolerance}{Maximum selection distance as a fraction of the
displayed view's bounding-box diagonal. Increase this when thin lines are
difficult to select.}
}
\value{
An edited \code{sf} object, or an atlas list with its selected layer
replaced. Closing the app without saving returns \code{x} unchanged.
}
\description{
Opens a Shiny editor for silhouette layers returned by
\code{\link[=build_brain_atlas]{build_brain_atlas()}}. Individual lines can be selected by clicking near
them, deleted, or smoothed with a live preview. Edits are only returned after
pressing \strong{Save and return to R}.
}
