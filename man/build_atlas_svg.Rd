% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_atlas.R
\name{build_atlas_svg}
\alias{build_atlas_svg}
\title{Build an sf atlas from labelled SVG layers}
\usage{
build_atlas_svg(
  svg_path,
  curve_steps = 20L,
  geometry_method = c("auto", "path", "concaveman"),
  concavity = 0,
  length_threshold = 2.5,
  flip_y = TRUE,
  combine_regions = TRUE
)
}
\arguments{
\item{svg_path}{Path to an SVG file.}

\item{curve_steps}{Number of straight segments used to approximate each
Bezier curve.}

\item{geometry_method}{Geometry construction method. \code{"path"} preserves the
authored boundary, \code{"concaveman"} reconstructs a boundary from sampled
points, and \code{"auto"} uses exact paths for labelled region drawings and
concaveman for legacy ID-only drawings.}

\item{concavity}{Concaveman concavity parameter. Smaller values produce a
more detailed boundary; \code{Inf} produces a convex hull.}

\item{length_threshold}{Concaveman segment-length threshold. Segments shorter
than this value are not refined further.}

\item{flip_y}{Whether to convert SVG's downward-positive Y coordinates to
conventional Cartesian coordinates.}

\item{combine_regions}{Whether groups with the same region name should be
combined into a single (possibly multipart) feature.}
}
\value{
An \code{sf} object with \code{region} and \code{geometry} columns.
}
\description{
\code{build_atlas_svg()} reads SVG groups containing paths, samples their line
and Bezier geometry, and creates one polygon per labelled region. Exact
closed paths are preserved; hull reconstruction remains available for
legacy SVGs whose paths represent converted strokes rather than regions.
}
\section{Authoring SVG atlas templates}{


New templates should encode the anatomical boundaries directly instead of
relying on hull reconstruction:
\enumerate{
\item Create one SVG group/layer for each anatomical region.
\item Draw the region as a \strong{closed, filled path}. Either the Bezier tool or a
freehand tool is suitable, provided the final object is an ordinary closed
path. The fill represents the region; a stroke is optional styling around
its boundary.
\item Give the region a stable, unique name. Names are resolved from \code{data-name},
a meaningful Inkscape layer label, a path \verb{<title>}, and finally the group
\code{id}. Generic labels such as \verb{Layer 2} are ignored when a path title is
available.
\item Keep each path as a direct child of its region layer. A layer may contain
multiple closed paths for a disconnected region. Alternatively, repeated
layers can use the same region name; \code{combine_regions = TRUE} combines
them into one multipart feature.
\item Do not convert strokes to paths. That creates a thin compound outline
rather than an anatomical polygon and requires reconstruction.
\item Convert live path effects and shape objects to ordinary paths before the
final export. Avoid clipping masks, clones, text, embedded raster images,
and nested decorative groups in atlas layers.
\item Save as plain or standard SVG. Line, cubic and quadratic Bezier path
commands are supported. Convert elliptical arcs to Bezier curves. The
importer supports \code{translate}, \code{scale}, \code{rotate}, and matrix transforms.
}

Import new templates with \code{geometry_method = "path"}. The \code{"auto"} method
treats a path \verb{<title>} as an exact-region marker; ID-only legacy drawings
are reconstructed with concaveman. Since concaveman is an optional suggested
dependency, install it only when legacy reconstruction is needed with
\code{install.packages("concaveman")}.

SVG coordinates have a downward-positive Y axis. \code{flip_y = TRUE} converts
them to the conventional Cartesian orientation used by \code{sf} and ggplot2.
}

\examples{
\dontrun{
atlas <- build_atlas_svg(
  "data/subcortical/drawings/Melbourne_S2_example.svg",
  geometry_method = "path"
)

# Only for legacy drawings whose paths are converted stroke outlines:
legacy_atlas <- build_atlas_svg(
  "data/subcortical/drawings/MTLfig_t2.svg",
  geometry_method = "concaveman",
  concavity = 0,
  length_threshold = 2.5
)
}
}
