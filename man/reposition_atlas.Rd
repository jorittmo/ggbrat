% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{reposition_atlas}
\alias{reposition_atlas}
\title{Reposition atlas panels with a text-based layout}
\usage{
reposition_atlas(x, layout, horizontal_gap = 0.05, vertical_gap = 0.05)
}
\arguments{
\item{x}{An \code{sf} atlas, or a list returned by \code{\link[=build_atlas_surf]{build_atlas_surf()}}.}

\item{layout}{A single character string describing the grid. Each non-empty
cell must be a letter assigned to one inferred panel. Newlines separate
rows; \code{.}, \verb{#}, and spaces denote empty cells.}

\item{horizontal_gap}{Horizontal distance between adjacent view bounding
boxes, in the coordinate units of the atlas.}

\item{vertical_gap}{Vertical distance between adjacent view bounding boxes,
in the coordinate units of the atlas.}
}
\value{
An object of the same form as \code{x}, with repositioned geometries. A
data frame mapping layout letters to panel metadata is stored in the
\code{layout_key} attribute.
}
\description{
Arranges atlas panels using a compact design string. Panels are inferred
from the atlas in the order in which they first appear and assigned the
letters \code{A}, \code{B}, \code{C}, and so on. For a surface atlas, a panel is each unique
\code{view} and \code{hemisphere} combination. For an atlas without multiple
hemispheres, including a volumetric atlas, a panel is each unique \code{view}.
}
\details{
For example, \code{"ABCD"} places four panels in one row, while \code{"AB\\nCD"}
creates a two-by-two grid. Use \code{.}, \verb{#}, or a space for an empty cell.
Letter matching is case-insensitive.

When \code{x} is the list returned by \code{\link[=build_atlas_surf]{build_atlas_surf()}}, the same translations
are applied to the \code{atlas}, \code{shade}, \code{silhouette}, \code{cortex}, and
\code{cortex_silhouette} layers. Camera positions and other non-spatial
components are left unchanged.
}
\examples{
\dontrun{
atlas <- load_atlas("Schaefer2018_400Parcels_7Networks_order")

# Put four panels in one row.
atlas <- reposition_atlas(atlas, layout = "ABCD")

# Put the same panels in a two-by-two grid.
atlas <- reposition_atlas(atlas, layout = "AB\nCD")

# Inspect which panel was assigned to each letter.
attr(atlas, "layout_key")
}
}
