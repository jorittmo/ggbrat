% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vol_atlas.R
\name{build_atlas_vol}
\alias{build_atlas_vol}
\title{Build orthogonal 2D atlases from volumetric NIfTI images}
\usage{
build_atlas_vol(
  atlas_path,
  lookup_path = NULL,
  gray_matter_path = NULL,
  gray_matter_threshold = 0.5,
  views = c("axial", "sagittal", "coronal"),
  slice_coordinates = c(axial = 0, sagittal = 0, coronal = 0),
  labels = NULL,
  include_gray_matter = TRUE,
  gray_matter_region = "gray_matter",
  exclude_atlas_from_gray_matter = TRUE,
  smooth_iterations = 0,
  gray_matter_smooth_iterations = smooth_iterations,
  interactive = FALSE,
  n_views = NULL
)
}
\arguments{
\item{atlas_path}{Path to a discrete-label NIfTI atlas.}

\item{lookup_path}{Optional CSV whose first two columns contain numeric labels
and region names. Headerless files are supported.}

\item{gray_matter_path}{Path to the gray-matter probability or anatomical
context NIfTI. When \code{NULL}, the MNI152NLin2009cAsym gray-matter
probability map is downloaded to and resolved from the ggbrat cache.}

\item{gray_matter_threshold}{Values greater than or equal to this threshold
are included in the context mask.}

\item{views}{Any ordered subset of \code{"axial"}, \code{"sagittal"}, and
\code{"coronal"}.}

\item{slice_coordinates}{Named world coordinates for the requested views.
Defaults to coordinate zero for every view.}

\item{labels}{Optional numeric atlas labels to include. The default includes
every nonzero label intersecting a selected slice.}

\item{include_gray_matter}{Whether to include the thresholded context region.}

\item{gray_matter_region}{Region name assigned to the context polygons.
May be \code{NA_character_} when the context should not be treated as an atlas
region.}

\item{exclude_atlas_from_gray_matter}{Whether atlas-labelled cells should be
removed from the context mask so the returned polygons form a nonoverlapping
categorical atlas.}

\item{smooth_iterations}{Strength of geometric outline smoothing, expressed
in voxel-width steps. Zero (the default) preserves exact voxel boundaries;
values from 1 to 3 progressively round atlas-region stair-step edges.
Fractional values are supported. Smoothing uses
morphological buffers followed by simplification and does not repeatedly
multiply the number of boundary vertices.}

\item{gray_matter_smooth_iterations}{Smoothing strength for the gray-matter
context outline. By default this inherits \code{smooth_iterations}; set it
separately when the cortex needs less smoothing than the atlas regions.}

\item{interactive}{If \code{TRUE}, open an interactive slice selector. The user
can switch anatomical axes, scroll through slices, and save \code{n_views}
selections before polygon construction begins. Requires the suggested
package \code{shiny}.}

\item{n_views}{Number of slices to save in interactive mode.}
}
\value{
An \code{sf} object with region polygons and columns \code{region}, \code{label},
\code{tissue}, \code{axis}, \code{int_view}, \code{view}, \code{selection_order}, \code{slice_index},
\code{slice_coordinate}, and \code{requested_coordinate}. \code{int_view} numbers slices
within each anatomical axis and \code{view} combines the axis and number, for
example \code{"axial_2"}. Use \code{ggplot2::facet_wrap(~view)} to display views
separately.
}
\description{
\code{build_atlas_vol()} combines a discrete-label subcortical atlas with a
thresholded gray-matter or anatomical context image and converts orthogonal
slices into \code{sf} polygons. Region boundaries are polygonized directly from
raster cells; no hull reconstruction is performed.
}
\details{
By default, the nearest voxel plane to world coordinate zero is selected for
axial, sagittal, and coronal views. The input images must currently share the
same dimensions and voxel-to-world affine transformation.
}
\examples{
\dontrun{
melbourne <- download_volume_atlas("Melbourne_S1")
atlas <- build_atlas_vol(
  atlas_path = melbourne$nifti,
  lookup_path = melbourne$lookup
)

ggplot2::ggplot(atlas) +
  ggplot2::geom_sf(ggplot2::aes(fill = region), linewidth = 0.2) +
  ggplot2::facet_wrap(~view) +
  ggplot2::theme_void()
}
}
