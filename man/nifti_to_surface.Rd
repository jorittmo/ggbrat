% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nifti_surface.R
\name{nifti_to_surface}
\alias{nifti_to_surface}
\title{Create a labelled surface mesh from a NIfTI atlas}
\usage{
nifti_to_surface(
  nifti_path,
  lookup_path = NULL,
  output_file = NULL,
  labels = NULL,
  split_hemispheres = FALSE,
  reduction = 0,
  subdivision = 0L,
  voxel_smoothing_sigma = 0,
  smoothing_method = c("windowed_sinc", "laplacian"),
  smoothing_iterations = 0L,
  smoothing_factor = 0.1,
  minimum_vertices = 4L,
  minimum_volume = 0.01,
  overwrite = FALSE
)
}
\arguments{
\item{nifti_path}{Path to a discrete-label \code{.nii} or \code{.nii.gz} image.}

\item{lookup_path}{Optional CSV whose first two columns contain numeric labels
and parcel names. Headerless files are supported.}

\item{output_file}{Destination \code{.vtp} file. By default it is named after the
NIfTI image under \code{data/subcortical/surfaces}.}

\item{labels}{Optional numeric labels to include. The default uses every
nonzero label in the image.}

\item{split_hemispheres}{Whether to create separate \verb{_left.vtp} and
\verb{_right.vtp} meshes. Hemispheres are inferred from parcel-name components
such as \verb{_L}, \verb{_R}, \code{left}, \code{right}, \code{lh}, and \code{rh} in the lookup table.}

\item{reduction}{Proportion of triangles to remove from each region, in
\verb{[0, 1)}. Cannot be combined with \code{subdivision}.}

\item{subdivision}{Number of linear subdivision levels from 0 through 3.
Each level creates approximately four times as many triangles. This adds
sampling vertices but does not add anatomical detail beyond the NIfTI.}

\item{voxel_smoothing_sigma}{Standard deviation, in voxels, for Gaussian
smoothing of each label's signed-distance field before marching cubes.
Zero preserves the exact voxel-derived boundary; values around 0.5 to 1.5
progressively reduce its staircase appearance.}

\item{smoothing_method}{Either \code{"windowed_sinc"}, which better preserves
volume, or \code{"laplacian"}, which more strongly rounds voxel edges.}

\item{smoothing_iterations}{Number of windowed-sinc smoothing iterations.}

\item{smoothing_factor}{Smoothing strength in \verb{(0, 1]}: the relaxation
factor for Laplacian smoothing or passband for windowed-sinc smoothing.}

\item{minimum_vertices}{Connected components with fewer vertices are removed.}

\item{minimum_volume}{Connected components with a smaller volume in cubic
world-coordinate units are removed.}

\item{overwrite}{Whether an existing output file may be replaced.}
}
\value{
A list containing \code{output_file}, per-region mesh summaries, and total
vertex and face counts.
}
\description{
Creates each nonzero atlas region independently using marching cubes, then
combines the disconnected components in one VTP file. The numeric \code{label}
and character \code{parcel} arrays are stored as both point and cell data.
}
